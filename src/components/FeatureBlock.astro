---
interface Props {
  title: string;
  description: string;
  isReversed?: boolean;
}

const { title, description, isReversed = false } = Astro.props;
---

<div class="py-20 first:pt-0 last:pb-0">
  <div class={`flex flex-col gap-16 items-center ${isReversed ? 'md:flex-row-reverse' : 'md:flex-row'}`}>
    <!-- Text Content -->
    <div class="flex-1 max-w-xl">
      <h3 class="text-3xl font-bold mb-6 bg-gradient-to-r from-white to-gray-300 bg-clip-text text-transparent">
        {title}
      </h3>
      <p class="text-lg leading-relaxed text-gray-400">
        {description}
      </p>
    </div>
    
    <!-- Visual Element -->
    <div class="flex-1">
      <div class="relative">
        <div class="absolute inset-0 bg-primary/20 blur-3xl rounded-full"></div>
        <div class="relative bg-white/5 border border-primary/10 rounded-2xl p-8 backdrop-blur-sm">
          <div class="aspect-[4/3] rounded-lg overflow-hidden bg-[#13151a] flex items-center justify-center">
            <div class="w-full max-w-xs">
              <div class="bg-white/10 rounded-lg p-4 mb-4">
                <div class="h-4 w-3/4 bg-white/20 rounded"></div>
              </div>
              <div class="space-y-3">
                <div class="h-3 w-full bg-white/10 rounded"></div>
                <div class="h-3 w-5/6 bg-white/10 rounded"></div>
                <div class="h-3 w-4/6 bg-white/10 rounded"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>